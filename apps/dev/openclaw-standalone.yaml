apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: openclaw-standalone
  namespace: devops
spec:
  destination:
    namespace: openclaw-standalone
    server: https://kubernetes.default.svc
  project: default
  source:
    repoURL: git@github.com:TikTzuki/tiktuzki-gitops.git
    targetRevision: main
    path: charts/openclaw-standalone
    helm:
      valueFiles:
        - values-dev.yaml
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
