persistence:
  size: 10Gi
  local:
    enabled: true
    node: node1
    path: /home/tik/data/openclaw
    reclaimPolicy: Retain

app-template:
  openclawVersion: "latest"

  controllers:
    main:
      containers:
        main:
          image:
            repository: tiktuzki/openclaw
            tag: slim
            pullPolicy: Always
          envFrom:
            - secretRef:
                name: openclaw-env
          env:
            OLLAMA_HOST: "http://ollama.ai-model.svc.cluster.local:11434"
          securityContext:
            readOnlyRootFilesystem: false
          resources:
            requests:
              cpu: "500m"
              memory: "1Gi"
            limits:
              cpu: "2"
              memory: "4Gi"

  configMaps:
    config:
      data:
        openclaw.json: |
          {
            "gateway": {
              "port": 18789,
              "mode": "local",
              "trustedProxies": ["192.168.1.5"],
              "controlUi": {
                "dangerouslyAllowHostHeaderOriginFallback": true
              }
            },
            "models": {
              "providers": {
                "ollama": {
                  "api": "ollama",
                  "apiKey": "ollama-local",
                  "baseUrl": "http://ollama.ai-model.svc.cluster.local:11434",
                  "models": [
                    {
                      "contextWindow": 32768,
                      "id": "qwen3:8b",
                      "maxTokens": 40960,
                      "name": "qwen3:8b"
                    }
                  ]
                }
              }
            },
            "agents": {
              "defaults": {
                "workspace": "/home/node/.openclaw/workspace",
                "model": {
                  "primary": "anthropic/claude-sonnet-4-6"
                },
                "userTimezone": "Asia/Ho_Chi_Minh",
                "compaction": {
                  "mode": "safeguard"
                }
              }
            },
            "channels": {
              "telegram": {
                "enabled": true,
                "botToken": "${TELEGRAM_BOT_TOKEN}",
                "configWrites": false,
                "dmPolicy": "allowlist",
                "allowFrom": [
                  1534702316
                ],
                "groups": {},
                "groupAllowFrom": [],
                "groupPolicy": "open",
                "streaming": "off",
                "actions": {
                  "sendMessage": true,
                  "sticker": true
                }
              }
            }
          }

  service:
    main:
      type: ClusterIP
      ports:
        http:
          port: 18789
          nodePort: 31789

  ingress:
    main:
      enabled: true
      className: public
      hosts:
        - host: openclaw.tiktuzki.com
          paths:
            - path: /
              pathType: Prefix
              service:
                identifier: main
                port: http

  persistence:
    data:
      enabled: true

  defaultPodOptions:
    nodeSelector:
      kubernetes.io/hostname: node1

