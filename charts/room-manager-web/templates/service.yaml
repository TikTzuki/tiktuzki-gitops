apiVersion: v1
kind: Service
metadata:
  name: {{ include "room-manager-web.fullname" . }}
  labels:
    {{- include "room-manager-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "room-manager-web.selectorLabels" . | nindent 4 }}
