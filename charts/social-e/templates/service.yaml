apiVersion: v1
kind: Service
metadata:
  name: {{ include "social-e.fullname" . }}
  labels:
    {{- include "social-e.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.backendPort }}
      targetPort: backend
      protocol: TCP
      name: backend
    - port: {{ .Values.service.frontendPort }}
      targetPort: frontend
      protocol: TCP
      name: frontend
  selector:
    {{- include "social-e.selectorLabels" . | nindent 4 }}
