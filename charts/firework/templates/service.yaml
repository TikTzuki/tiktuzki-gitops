apiVersion: v1
kind: Service
metadata:
  name: {{ include "firework.fullname" . }}
  labels:
    {{- include "firework.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "firework.selectorLabels" . | nindent 4 }}
