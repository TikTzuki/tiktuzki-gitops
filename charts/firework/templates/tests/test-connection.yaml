apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "firework.fullname" . }}-test-connection"
  labels:
    {{- include "firework.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: [ 'wget' ]
      args: [ '{{ include "firework.fullname" . }}:{{ .Values.service.port }}' ]
  restartPolicy: Never
