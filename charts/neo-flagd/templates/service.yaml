apiVersion: v1
kind: Service
metadata:
  name: {{ include "neo-flagd.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "neo-flagd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "neo-flagd.selectorLabels" . | nindent 4 }}
